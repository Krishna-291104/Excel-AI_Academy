services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: excelai
      POSTGRES_USER: excelai
      POSTGRES_PASSWORD: excelai_password
    ports: ["5432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
volumes:
  pgdata:
